# 塗装色混合アシスタント v2.0 - 統合設計書

## アーキテクチャ概要

### システム構成（実装済み）
```
┌─────────────────────────────────────────────────────────────┐
│                    Electron Shell (Optional)               │
├─────────────────────────────────────────────────────────────┤
│  Frontend (React + TypeScript + Vite) ✅ 実装済み           │
│  ┌─────────────────┐  ┌─────────────────┐                  │
│  │   Components    │  │     Hooks       │                  │
│  │ - ImageUpload   │  │ - useResponsive │ ✅ 実装済み       │
│  │ - ColorControl  │  │ - useColor      │                  │
│  │ - MixingDisplay │  │ - useImage      │                  │
│  │ - PaintMixing   │  │                 │                  │
│  │ - CompactBar    │  │                 │                  │
│  └─────────────────┘  └─────────────────┘                  │
│  ┌─────────────────┐  ┌─────────────────┐                  │
│  │     Utils       │  │     Types       │                  │
│  │ - colorUtils    │  │ - color.ts      │ ✅ 実装済み       │
│  │ - paintMixing   │  │ - image.ts      │                  │
│  │ - electronUtils │  │ - ui.ts         │                  │
│  └─────────────────┘  └─────────────────┘                  │
├─────────────────────────────────────────────────────────────┤
│  クライアント完結型アーキテクチャ ✅ 実装済み                  │
│  - サーバー通信なしのリアルタイム色調整                        │
│  - Canvas APIによる画像処理                                  │
│  - GitHub Pagesでの静的サイトホスティング                     │
└─────────────────────────────────────────────────────────────┘
```

### 画面構成（実装済み）
```
┌─────────────────────────────────────────────────────────────┐
│                        Header                               │
│  塗装色混合アシスタント v2.0 + デバイス情報                    │
├─────────────────────────────────────────────────────────────┤
│                    Main Content                             │
│  ┌─────────────────┐  ┌─────────────────┐                  │
│  │   Left Column   │  │  Right Column   │ (Desktop)        │
│  │ - ImageUpload A │  │ - ImageUpload B │                  │
│  │ - ColorCtrl A   │  │ - ColorCtrl B   │                  │
│  └─────────────────┘  └─────────────────┘                  │
│  ┌─────────────────────────────────────────┐                │
│  │           Mixing Section                │                │
│  │ ┌─────────────────┐ ┌─────────────────┐ │                │
│  │ │ ColorMixingDisp │ │ PaintMixingCtrl │ │                │
│  │ │ (必要な塗料調整) │ │ (混色コントローラ)│ │                │
│  │ └─────────────────┘ └─────────────────┘ │                │
│  └─────────────────────────────────────────┘                │
├─────────────────────────────────────────────────────────────┤
│                        Footer                               │
└─────────────────────────────────────────────────────────────┘
```

### 実装画面レイアウト（画面サンプル.png参照）
```
┌─────────────────────────────────────────────────────────────┐
│ 塗装色混合アシスタント v2.0 - Node.js + React + TypeScript版  │
│ クライアント側完結の色調整 | デバイス: desktop | デスクトップ: Yes│
├─────────────────────────────────────────────────────────────┤
│ ✅ 基本表示テスト                                            │
│ • React コンポーネント: 正常                                  │
│ • CSS スタイル: 適用済み                                      │
│ • TypeScript: コンパイル成功                                  │
│ • レスポンシブフック: 動作中                                   │
├─────────────────────────────────────────────────────────────┤
│ 🖼️ 画像アップロード A テスト    │ 🖼️ 画像アップロード B テスト    │
│ ┌─────────────────────────────┐ │ ┌─────────────────────────────┐ │
│ │ PATH: [入力フィールド]        │ │ │ PATH: [入力フィールド]        │ │
│ │ [ファイル選択] [読み込み]      │ │ │ [ファイル選択] [読み込み]      │ │
│ │ ドラッグ&ドロップエリア        │ │ │ ドラッグ&ドロップエリア        │ │
│ │ [画像プレビューエリア]        │ │ │ [画像プレビューエリア]        │ │
│ └─────────────────────────────┘ │ └─────────────────────────────┘ │
│                                │                                │
│ 🎨 色調コントローラ A テスト      │ 🎨 色調コントローラ B テスト      │
│ ┌─────────────────────────────┐ │ ┌─────────────────────────────┐ │
│ │ 出発色: [色見本] RGB/CMYK値   │ │ │ 出発色: [色見本] RGB/CMYK値   │ │
│ │ 結果色: [色見本] RGB/CMYK値   │ │ │ 結果色: [色見本] RGB/CMYK値   │ │
│ │ R: [スライダー] G: [スライダー] │ │ │ R: [スライダー] G: [スライダー] │ │
│ │ B: [スライダー]              │ │ │ B: [スライダー]              │ │
│ │ C: [スライダー] M: [スライダー] │ │ │ C: [スライダー] M: [スライダー] │ │
│ │ Y: [スライダー] K: [スライダー] │ │ │ Y: [スライダー] K: [スライダー] │ │
│ └─────────────────────────────┘ │ └─────────────────────────────┘ │
├─────────────────────────────────────────────────────────────┤
│ 🎨 必要な塗料調整              │ 🎨 混色コントローラ              │
│ ┌─────────────────────────────┐ │ ┌─────────────────────────────┐ │
│ │ シアン   ■■■■ +44%         │ │ │ 出発色    算出色              │ │
│ │ マゼンタ ■■   +22%         │ │ │ [色見本]  [色見本]            │ │
│ │ イエロー ■     +8%          │ │ │                             │ │
│ │ 黒      ■■■   +33%         │ │ │ CMYK調整                     │ │
│ │ 白      ■■■■■ +55%        │ │ │ シアン   [44] → 88           │ │
│ └─────────────────────────────┘ │ │ マゼンタ [22] → 44           │ │
│                                │ │ イエロー [8]  → 16           │ │
│                                │ │ 黒      [33] → 66           │ │
│                                │ │ 白      [55] → 55           │ │
│                                │ │                             │ │
│                                │ │ 目的色    結果色              │ │
│                                │ │ [色見本]  [色見本]            │ │
│                                │ └─────────────────────────────┘ │
├─────────────────────────────────────────────────────────────┤
│ © 2025 塗装色混合アシスタント - Powered by Kiro AI | BtoBテスト実行中│
└─────────────────────────────────────────────────────────────┘
```

## フロントエンド設計

### コンポーネント階層
```
App
├── Header (title, subtitle, device info)
├── Main
│   ├── Desktop Layout (2-column)
│   │   ├── Left Column
│   │   │   ├── ImageUpload (A)
│   │   │   └── ColorController (A)
│   │   └── Right Column
│   │       ├── ImageUpload (B)
│   │       └── ColorController (B)
│   ├── Mobile Layout (vertical stack)
│   │   ├── ImageUpload (A)
│   │   ├── ColorController (A)
│   │   ├── ImageUpload (B)
│   │   └── ColorController (B)
│   ├── ImageSwapSection
│   │   └── ImageSwapButton
│   └── Mixing Section
│       ├── ColorMixingDisplay
│       └── PaintMixingController
└── Footer
```

### 状態管理設計
```typescript
// メインアプリケーション状態
interface AppState {
  // 色A（現在の色）
  originalColorA: ColorModel;    // 出発色（基準）
  resultColorA: ColorModel;      // 調整結果色
  
  // 色B（目標の色）
  originalColorB: ColorModel;    // 出発色（基準）
  resultColorB: ColorModel;      // 調整結果色
  
  // 画像状態（画像交換機能対応）
  imageDataA: AppImageData | null;  // 画像Aのデータ
  imageDataB: AppImageData | null;  // 画像Bのデータ
  imagePathA: string;               // 画像AのPATH
  imagePathB: string;               // 画像BのPATH
  imageUpdateKeyA: number;          // 画像A更新キー
  imageUpdateKeyB: number;          // 画像B更新キー
  
  // 混色結果
  mixingResultColor: ColorModel; // 混色コントローラーの結果
  calculatedColor: ColorModel;   // 逆算結果色
  
  // UI状態
  deviceType: DeviceType;        // デバイス種別
  isDesktop: boolean;           // デスクトップ判定
}
```

### 色モデル設計
```typescript
interface ColorModel {
  // RGB値 (0-255)
  r: number;
  g: number;
  b: number;
  
  // CMYK値 (0-100%)
  c: number;
  m: number;
  y: number;
  k: number;
}
```

## コンポーネント設計

### コンポーネント流用ガイド

#### 🎯 ColorController コンポーネントの流用方法

**再利用可能性**: ★★★★★（90%）- 高い汎用性

**必要ファイル**:
```
src/components/ColorController/
├── ColorController.tsx      # メインコンポーネント
├── ColorController.css      # スタイル定義
src/types/color.ts          # 型定義
src/utils/colorUtils.ts     # 色変換ユーティリティ
src/hooks/useResponsiveLayout.ts # レスポンシブフック
```

**流用手順**:
1. **ファイルコピー**: 上記5ファイルを新プロジェクトにコピー
2. **依存関係インストール**: React 18+ TypeScript 4.5+
3. **プロパティ設定**: 以下のプロパティを外部から制御
   ```typescript
   <ColorController
     originalColor={baseColor}        // 出発色（ColorModel型）
     resultColor={adjustedColor}      // 結果色（ColorModel型）
     onChange={handleColorChange}     // 変更コールバック
     label="色調整"                   // 表示ラベル
     disabled={false}                 // 無効化フラグ（オプション）
   />
   ```

4. **カスタマイズポイント**:
   - **ラベル変更**: `label`プロパティで表示名をカスタマイズ
   - **スタイル調整**: `ColorController.css`でデザイン変更
   - **機能制限**: `disabled`プロパティで操作無効化
   - **レスポンシブ**: 自動対応（`useResponsiveLayout`フック使用）

**使用例**:
```typescript
// 複数の色調整コンポーネントを並列使用
const [colorA, setColorA] = useState<ColorModel>(initialColorA);
const [colorB, setColorB] = useState<ColorModel>(initialColorB);

return (
  <div className="color-adjustment-panel">
    <ColorController
      originalColor={originalColorA}
      resultColor={colorA}
      onChange={setColorA}
      label="画像A色調整"
    />
    <ColorController
      originalColor={originalColorB}
      resultColor={colorB}
      onChange={setColorB}
      label="画像B色調整"
    />
  </div>
);
```

#### 🎨 PaintMixingController コンポーネントの流用方法（v2.1更新）

**再利用可能性**: ★★★★☆（80%）- 塗料混合特化 + モバイルUX改善

**必要ファイル**:
```
src/components/PaintMixingController/
├── PaintMixingController.tsx    # メインコンポーネント（◀▶ボタン対応）
├── PaintMixingController.css    # スタイル定義（モバイル最適化）
src/types/color.ts              # 色型定義
src/types/paintMixing.ts        # 塗料混合型定義
src/utils/colorUtils.ts         # 色変換ユーティリティ
src/utils/paintMixing.ts        # 塗料混合計算
src/hooks/useResponsiveLayout.ts # レスポンシブフック（◀▶ボタン表示制御）
src/hooks/useLongPress.ts       # 長押しフック（◀▶ボタン機能）
```

**v2.1の新機能**:
- **◀▶ボタン**: モバイル表示時のみ表示される直感的な操作ボタン
- **長押し機能**: 連続調整による効率的な塗料調整
- **タッチ最適化**: 44px×44px以上のタッチ領域確保
- **UI一貫性**: ColorControllerと統一されたデザイン

**流用手順**:
1. **ファイルコピー**: 上記8ファイルを新プロジェクトにコピー
2. **依存関係確認**: React 18+ TypeScript 4.5+
3. **プロパティ設定**: 4つの色を外部から制御
   ```typescript
   <PaintMixingController
     baseColor={sourceColor}          // 出発色（ColorModel型）
     targetColor={goalColor}          // 目標色（ColorModel型）
     calculatedColor={computedColor}  // 算出色（ColorModel型）
     onChange={handleResultChange}    // 結果色変更コールバック
   />
   ```

4. **カスタマイズポイント**:
   - **◀▶ボタン制御**: `useResponsiveLayout`でモバイル判定
   - **長押し設定**: `useLongPress`の増減量・間隔調整
   - **レイアウト調整**: CSS Grid/Flexboxレイアウトの変更
   - **タッチ領域**: ボタンサイズのカスタマイズ

**v2.1の制約事項**:
- **固定レイアウト**: 4箇所の色配置（出発色・目標色・算出色・結果色）
- **CMYK特化**: RGB調整機能なし
- **塗料混合専用**: 汎用的な色調整には不向き
- **モバイル依存**: ◀▶ボタンはモバイル表示でのみ利用可能

**使用例（v2.1対応）**:
```typescript
// 塗料混合シミュレーション（◀▶ボタン対応）
const [mixingResult, setMixingResult] = useState<ColorModel>();
const { isMobile } = useResponsiveLayout(); // モバイル判定

// 必要な塗料調整から算出色を計算
const calculatedColor = useMemo(() => {
  return PaintMixingCalculator.calculateReverseMixingColor(baseColor, targetColor);
}, [baseColor, targetColor]);

return (
  <div className="paint-mixing-section">
    <PaintMixingController
      baseColor={baseColor}
      targetColor={targetColor}
      calculatedColor={calculatedColor}
      onChange={setMixingResult}
    />
    {isMobile && (
      <p className="mobile-hint">◀▶ボタンで直感的に調整できます</p>
    )}
  </div>
);
```

#### 🔧 共通ユーティリティの流用方法

**ColorSpaceConverter（色空間変換）**:
```typescript
// RGB→CMYK変換
const cmyk = ColorSpaceConverter.rgbToCmyk(255, 128, 64);

// CMYK→RGB変換
const rgb = ColorSpaceConverter.cmykToRgb(25, 50, 75, 10);

// 安全な色更新（変換ループ防止）
const updatedColor = ColorSpaceConverter.safeColorUpdate(
  currentColor, 
  newValues, 
  'rgb'
);
```

**PaintMixingCalculator（塗料混合計算）**:
```typescript
// 混合比率計算
const mixingResult = PaintMixingCalculator.calculateMixingRatio(
  colorA, 
  colorB
);

// 逆算結果色計算
const reversedColor = PaintMixingCalculator.calculateReverseMixingColor(
  baseColor, 
  targetColor
);
```

**useResponsiveLayout（レスポンシブフック）**:
```typescript
const { deviceType, isMobile, isDesktop } = useResponsiveLayout();

// デバイス別レンダリング
return (
  <div className={`layout ${deviceType}`}>
    {isDesktop ? <DesktopLayout /> : <MobileLayout />}
  </div>
);
```

#### 📋 流用時の注意事項

**型定義の依存関係**:
- `ColorModel`インターフェースは必須
- RGB値（0-255）とCMYK値（0-100%）の統合型
- 他の色型（`RgbColor`, `CmykColor`）も必要に応じてコピー

**CSS依存関係**:
- CSS GridとFlexboxを使用
- CSS Custom Properties（CSS変数）を活用
- レスポンシブブレークポイント: 768px（モバイル）, 1024px（タブレット）

**パフォーマンス考慮**:
- `React.memo`でメモ化済み
- `useCallback`で関数メモ化済み
- `useMemo`で計算結果キャッシュ済み

**ブラウザ対応**:
- モダンブラウザ対応（ES2020+）
- CSS Grid/Flexbox必須
- Canvas API使用（画像処理時）

### 1. ImageUpload コンポーネント
```typescript
interface ImageUploadProps {
  onImageSelect: (image: ImageData) => void;
  onColorSelect: (color: ColorModel) => void;
  deviceType: DeviceType;
  label: string;
}
```

**機能**:
- PATH入力フィールド
- ファイル選択ボタン
- ドラッグ&ドロップエリア
- Canvas画像プレビュー
- クリック色選択

**状態管理**:
- 画像データ（Base64）
- プレビュー状態
- エラー状態

### 2. ColorController コンポーネント
```typescript
interface ColorControllerProps {
  originalColor: ColorModel;  // 出発色（表示のみ）
  resultColor: ColorModel;    // 結果色（調整対象）
  onChange: (color: ColorModel) => void;
  label: string;
  disabled?: boolean;
}
```

**機能**:
- RGB値調整スライダー（0-255）
- CMYK値調整スライダー（0-100%）
- 色プレビュー表示
- 出発色との比較表示

**色変換ロジック**:
- RGB→CMYK変換時: `ColorSpaceConverter.rgbToCmyk()`
- CMYK→RGB変換時: `ColorSpaceConverter.cmykToRgb()`
- 変換ループ防止: `ColorSpaceConverter.safeColorUpdate()`

### 3. ColorMixingDisplay コンポーネント
```typescript
interface ColorMixingDisplayProps {
  colorA: ColorModel;  // 現在の色
  colorB: ColorModel;  // 目標の色
}
```

**機能**:
- 色A・色B・混合結果の並列表示
- RGB/CMYK値の数値表示
- 色差の視覚的表示

### 4. PaintMixingController コンポーネント（実装済み）
```typescript
interface PaintMixingControllerProps {
  baseColor: ColorModel;        // 出発色
  targetColor: ColorModel;      // 目標色
  calculatedColor: ColorModel;  // 逆算結果色
  onChange: (color: ColorModel) => void;
}
```

**機能**:
- 必要な塗料調整の表示 ✅ 実装済み
- 各塗料の追加量バー表示 ✅ 実装済み
- 逆算結果色のプレビュー ✅ 実装済み
- 調整指示テキスト ✅ 実装済み
- **リアルタイム混色計算** ✅ 実装済み
- **白黒相互作用表示** ✅ 実装済み
- **CMYK調整前後の値表示** ✅ 実装済み

### 5. CompactMixingBar コンポーネント（実装済み）
```typescript
interface CompactMixingBarProps {
  label: string;     // 塗料名
  value: number;     // 調整量
  maxValue?: number; // 最大値
}
```

**機能**:
- コンパクトな1行表示 ✅ 実装済み
- 視覚的バーグラフ（■記号） ✅ 実装済み
- 色インジケーター ✅ 実装済み
- 符号付き数値表示 ✅ 実装済み

### 6. ImageSwapButton コンポーネント（実装済み）
```typescript
interface ImageSwapButtonProps {
  onClick: () => void;           // クリック時のコールバック
  disabled?: boolean;            // 無効化フラグ
  className?: string;            // カスタムクラス名
  'aria-label'?: string;         // アクセシビリティラベル
}
```

**機能**:
- 画像交換ボタンの表示 ✅ 実装済み
- ワンクリック交換操作 ✅ 実装済み
- レスポンシブ対応 ✅ 実装済み
- アクセシビリティ対応 ✅ 実装済み
- 中央配置レイアウト ✅ 実装済み

**レイアウト設計**:
```css
.image-swap-section {
  width: 100%;
  display: flex;
  justify-content: center;
  margin: 0;
  padding: 1rem 0;
}

.image-swap-button {
  padding: 0.75rem 1.5rem;
  background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
  color: white;
  border: none;
  border-radius: 0.5rem;
  font-weight: 600;
  cursor: pointer;
  transition: all 0.2s ease;
}
```

**交換処理フロー**:
```typescript
const handleImageSwap = useCallback(() => {
  // 1. 色の交換
  const tempOriginalA = originalColorA;
  const tempResultA = resultColorA;
  setOriginalColorA(originalColorB);
  setResultColorA(resultColorB);
  setOriginalColorB(tempOriginalA);
  setResultColorB(tempResultA);
  
  // 2. 画像データの交換
  const tempImageDataA = imageDataA;
  const tempImagePathA = imagePathA;
  setImageDataA(imageDataB);
  setImagePathA(imagePathB);
  setImageDataB(tempImageDataA);
  setImagePathB(tempImagePathA);
  
  // 3. 更新キーの変更（再描画トリガー）
  setImageUpdateKeyA(prev => prev + 1);
  setImageUpdateKeyB(prev => prev + 1);
}, [/* 依存配列 */]);
```

## 塗料混合計算設計（実装済み）

### 計算アルゴリズム
```typescript
class PaintMixingCalculator {
  // メイン計算メソッド ✅ 実装済み
  static calculateMixingRatio(colorA: ColorModel, colorB: ColorModel): MixingResult
  
  // 逆算結果色計算 ✅ 実装済み
  static calculateReverseMixingColor(baseColor: ColorModel, targetColor: ColorModel): ColorModel
}

class UnifiedPaintMixer {
  // 統一された混色計算 ✅ 実装済み
  static calculateMixing(baseColor: ColorModel, adjustments: MixingAdjustments): ColorModel
}
```

### 計算ロジック（実装済み）
1. **CMYK差分計算**: `colorB.cmyk - colorA.cmyk` ✅ 実装済み
2. **塗料追加処理**: ✅ 実装済み
   - 正の差分 → 該当塗料を追加
   - 負の差分 → 補色塗料を追加（中和）
3. **白・黒処理**: ✅ 実装済み
   - K値増加 → 黒塗料追加
   - K値減少 → 白塗料追加
4. **統合処理**: 同一塗料の指示を合計 ✅ 実装済み
5. **逆算適用**: 計算結果をCMYK値に適用してRGB変換 ✅ 実装済み

### 補色理論（実装済み）
- シアン減少 → マゼンタ + イエロー追加 ✅ 実装済み
- マゼンタ減少 → シアン + イエロー追加 ✅ 実装済み
- イエロー減少 → シアン + マゼンタ追加 ✅ 実装済み

### 高度な機能（実装済み）
- **白黒相互作用**: 白と黒の同時使用によるグレー効果 ✅ 実装済み
- **彩度低下計算**: グレー効果による彩度低下の計算 ✅ 実装済み
- **リアルタイム計算**: 調整値変更時の即座な結果反映 ✅ 実装済み

## Electron統合設計

### プロセス間通信（IPC）
```javascript
// Main Process (main.js)
ipcMain.handle('show-open-dialog', async () => { ... });
ipcMain.handle('load-image-from-path', async (event, filePath) => { ... });

// Preload Script (preload.js)
contextBridge.exposeInMainWorld('electronAPI', {
  showOpenDialog: () => ipcRenderer.invoke('show-open-dialog'),
  loadImageFromPath: (filePath) => ipcRenderer.invoke('load-image-from-path', filePath),
  platform: process.platform,
  isElectron: true
});
```

### ファイルアクセス設計
1. **ファイル選択**: OS標準ダイアログ → Base64変換 → レンダラープロセス
2. **PATH読み込み**: パス検証 → ファイル読み込み → Base64変換
3. **セキュリティ**: contextBridgeによる安全なAPI公開

### 開発・本番環境切り替え
```javascript
const isDev = process.env.NODE_ENV === 'development';

if (isDev) {
  // 開発環境: Viteサーバー接続
  await mainWindow.loadURL('http://localhost:5173');
  mainWindow.webContents.openDevTools();
} else {
  // 本番環境: 静的ファイル読み込み
  await mainWindow.loadFile('../frontend/dist/index.html');
}
```

## バックエンド設計

### API エンドポイント
```typescript
// ヘルスチェック
GET /api/health
Response: { status: 'ok', message: string, timestamp: string, version: string }

// 画像アップロード
POST /api/upload
Body: FormData (image file)
Response: { message: string, file: FileInfo }

// PATH画像読み込み（制限付き）
POST /api/load-image-path
Body: { filePath: string }
Response: { error: string, message: string, suggestion: string }

// 色変換（将来拡張用）
POST /api/color/convert
Body: { r, g, b, c, m, y, k, type }
Response: { message: string, clientSide: true }

// 塗料混合計算（将来拡張用）
POST /api/paint/mixing
Body: { colorA: ColorModel, colorB: ColorModel }
Response: { message: string, clientSide: true }
```

### ミドルウェア構成
1. **CORS**: フロントエンドからのアクセス許可
2. **Body Parser**: JSON/URLエンコード対応
3. **Multer**: ファイルアップロード処理
4. **Static Files**: アップロード画像の配信
5. **Error Handler**: 統一エラー処理

## レスポンシブデザイン設計

### ブレークポイント
```typescript
const BREAKPOINTS = {
  mobile: 768,
  tablet: 1024,
  desktop: 1200
};
```

### レイアウト切り替え
```typescript
const useResponsiveLayout = () => {
  const [deviceType, setDeviceType] = useState<DeviceType>('desktop');
  const [isDesktop, setIsDesktop] = useState(true);
  
  useEffect(() => {
    const handleResize = () => {
      const width = window.innerWidth;
      if (width < BREAKPOINTS.mobile) {
        setDeviceType('mobile');
        setIsDesktop(false);
      } else if (width < BREAKPOINTS.tablet) {
        setDeviceType('tablet');
        setIsDesktop(false);
      } else {
        setDeviceType('desktop');
        setIsDesktop(true);
      }
    };
    
    handleResize();
    window.addEventListener('resize', handleResize);
    return () => window.removeEventListener('resize', handleResize);
  }, []);
  
  return { deviceType, isDesktop };
};
```

### CSS設計
```css
/* デスクトップ: 2カラムレイアウト */
.app__desktop-layout {
  display: flex;
  gap: 2rem;
}

.app__column {
  flex: 1;
  display: flex;
  flex-direction: column;
  gap: 1.5rem;
}

/* モバイル: 縦積みレイアウト */
.app__mobile-layout {
  display: flex;
  flex-direction: column;
  gap: 1.5rem;
}

/* 混色セクション: 横並び */
.app__mixing-section {
  display: flex;
  gap: 2rem;
  margin-top: 2rem;
}

@media (max-width: 768px) {
  .app__mixing-section {
    flex-direction: column;
  }
}
```

## データフロー設計

### 色選択フロー
```
画像クリック → onColorSelect → 
出発色・結果色を同じ値に設定 → 
ColorController更新 → 
塗料混合計算実行 → 
結果表示更新
```

### 色調整フロー
```
RGB/CMYKスライダー操作 → onChange → 
結果色のみ更新 → 
色空間変換実行 → 
塗料混合計算実行 → 
結果表示更新
```

### 塗料混合計算フロー
```
resultColorA + resultColorB → 
PaintMixingCalculator.calculateMixingRatio → 
MixingResult生成 → 
PaintMixingCalculator.calculateReverseMixingColor → 
逆算結果色生成 → 
UI表示更新
```

### 画像交換フロー（新規追加）
```
[画像を交換]ボタンクリック → handleImageSwap → 
色の交換（originalColorA ⇔ originalColorB, resultColorA ⇔ resultColorB） → 
画像データの交換（imageDataA ⇔ imageDataB, imagePathA ⇔ imagePathB） → 
更新キーの変更（imageUpdateKeyA++, imageUpdateKeyB++） → 
ImageUploadコンポーネントの再描画 → 
ColorControllerコンポーネントの更新 → 
塗料混合計算の再実行 → 
全体UI更新
```

## 正確性プロパティ（Correctness Properties）

*プロパティとは、システムのすべての有効な実行において真であるべき特性や動作のことです。これらは人間が読める仕様と機械で検証可能な正確性保証の橋渡しとなります。*

### 塗料混合計算プロパティ

**プロパティ 1: 混色コントローラの算出色一致**
*任意の*混色コントローラでの調整において、「必要な塗料調整」の値を入力した時、結果色が算出色と完全一致する
**Validates: Requirements 6.4**

**プロパティ 2: 補色理論の適用**
*任意の*負のCMYK差分について、補色塗料の追加により物理的制約（加算のみ）を遵守した計算が行われる
**Validates: Requirements 6.1**

**プロパティ 3: 白黒相互作用の計算**
*任意の*白と黒の同時使用において、グレー効果による彩度低下が正しく計算される
**Validates: Requirements 6.2**

### UI表示プロパティ

**プロパティ 4: レスポンシブレイアウト**
*任意の*画面幅において、適切なブレークポイント（1024px）でレイアウトが切り替わる
**Validates: Requirements 4.1**

**プロパティ 5: コンパクト表示の一貫性**
*任意の*塗料調整において、CompactMixingBarの表示形式が「[色名] [バーグラフ] [数値]」で統一される
**Validates: Requirements 4.3**

### 色空間変換プロパティ

**プロパティ 6: RGB⇔CMYK変換の往復一致性**
*任意の*有効な色について、RGB→CMYK→RGB変換の結果が元の色と一致する（許容誤差内）
**Validates: Requirements 2.1.2**

### モバイルUX改善プロパティ

**プロパティ 7: ◀▶ボタンの増減一貫性**
*任意の*CMYK数値入力フィールドについて、◀ボタンで1%減少、▶ボタンで1%増加が正確に実行される
**Validates: Requirements 7.1**

**プロパティ 8: 長押し操作の連続性**
*任意の*◀▶ボタンについて、長押し中は一定間隔で数値が連続的に増減する
**Validates: Requirements 7.1**

**プロパティ 9: 数値範囲の境界保護**
*任意の*CMYK値について、◀▶ボタン操作後も0-100%の範囲内に収まる
**Validates: Requirements 7.2**

**プロパティ 10: モバイル表示での◀▶ボタン表示**
*任意の*画面幅768px未満において、混色コントローラの各CMYK成分に◀▶ボタンが表示される
**Validates: Requirements 7.1**

### 画像交換機能プロパティ

**プロパティ 11: 画像交換の完全性**
*任意の*画像交換操作において、画像A・Bのプレビュー、色調整値、ファイルパスが完全に交換される
**Validates: Requirements 5.2**

**プロパティ 12: 画像交換後の状態同期**
*任意の*画像交換操作後において、交換されたデータが即座に画面に反映される
**Validates: Requirements 5.2**

**プロパティ 13: 画像交換ボタンの配置一貫性**
*任意の*デバイス環境において、画像交換ボタンが色調コントローラと塗料混合セクションの間に中央配置される
**Validates: Requirements 5.1**

## テスト戦略

### デュアルテストアプローチ
- **単体テスト**: 特定の例、エッジケース、エラー条件を検証
- **プロパティテスト**: すべての入力にわたる普遍的プロパティを検証
- **両方が補完的**: 単体テストは具体的なバグを捕捉、プロパティテストは一般的な正確性を検証

### プロパティベーステスト設定
- **テストライブラリ**: Jest + @fast-check/jest（TypeScript用）
- **最小反復回数**: 100回（ランダム化による）
- **テストタグ**: **Feature: paint-color-assistant, Property {番号}: {プロパティテキスト}**
- **各正確性プロパティ**: 単一のプロパティベーステストで実装

## エラーハンドリング設計

### フロントエンド
// 画像読み込みエラー
try {
  const result = await window.electronAPI.loadImageFromPath(filePath);
  if (!result.success) {
    setError(result.error);
    return;
  }
} catch (error) {
  setError('ファイル読み込みに失敗しました');
}

// 色変換エラー
try {
  const cmyk = ColorSpaceConverter.rgbToCmyk(r, g, b);
} catch (error) {
  console.error('色変換エラー:', error);
  // フォールバック処理
}
```

### バックエンド
```typescript
// 統一エラーハンドラー
app.use((error: any, req: express.Request, res: express.Response, next: express.NextFunction) => {
  console.error('サーバーエラー:', error);
  
  res.status(500).json({
    error: 'サーバー内部エラーが発生しました',
    message: error.message
  });
});
```

### Electron
```javascript
// ファイル読み込みエラー
try {
  const imageBuffer = fs.readFileSync(filePath);
  // 処理続行
} catch (error) {
  return { success: false, error: error.message };
}
```

## パフォーマンス最適化

### フロントエンド最適化
1. **React.memo**: 不要な再レンダリング防止
2. **useCallback**: 関数の再生成防止
3. **useMemo**: 計算結果のキャッシュ
4. **debounce**: スライダー操作の最適化

### 画像処理最適化
1. **Canvas最適化**: 適切なサイズでの描画
2. **Base64キャッシュ**: 画像データの再利用
3. **メモリ管理**: 不要なオブジェクトの解放

### バックエンド最適化
1. **ファイルサイズ制限**: 10MB上限
2. **メモリ効率**: Streamベースの処理
3. **エラー早期返却**: 不正リクエストの早期検出

## セキュリティ設計

### Electron セキュリティ
1. **nodeIntegration: false**: Node.js APIの無効化
2. **contextIsolation: true**: コンテキスト分離
3. **preload script**: 安全なAPI公開
4. **ファイル検証**: 拡張子・MIMEタイプ確認

### バックエンド セキュリティ
1. **CORS設定**: 許可オリジンの制限
2. **ファイル検証**: アップロードファイルの検証
3. **パス検証**: ディレクトリトラバーサル防止
4. **サイズ制限**: DoS攻撃防止

## テスト設計

### 単体テスト
```typescript
// 色変換テスト
describe('ColorSpaceConverter', () => {
  test('RGB to CMYK conversion', () => {
    const cmyk = ColorSpaceConverter.rgbToCmyk(255, 0, 0);
    expect(cmyk).toEqual({ c: 0, m: 100, y: 100, k: 0 });
  });
});

// 塗料混合計算テスト
describe('PaintMixingCalculator', () => {
  test('mixing ratio calculation', () => {
    const result = PaintMixingCalculator.calculateMixingRatio(colorA, colorB);
    expect(result.instructions).toHaveLength(expectedLength);
  });
});
```

### E2Eテスト
```typescript
// Puppeteer/Seleniumによるブラウザテスト
describe('Image Upload Flow', () => {
  test('should upload and preview image', async () => {
    await page.click('[data-testid="file-input"]');
    await page.setInputFiles('input[type="file"]', 'test-image.png');
    await expect(page.locator('[data-testid="image-preview"]')).toBeVisible();
  });
});
```

## 配布・デプロイ設計

### GitHub Pages（Web版）
```yaml
# .github/workflows/deploy.yml
name: Deploy to GitHub Pages
on:
  push:
    branches: [ master ]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '18'
    - name: Build
      run: |
        cd frontend
        npm ci
        npm run build
    - name: Deploy
      uses: actions/deploy-pages@v4
```

### Electron配布
```json
// package.json
{
  "scripts": {
    "electron:build": "npm run build:frontend && electron-builder",
    "electron:dist": "npm run electron:build -- --publish=never"
  },
  "build": {
    "appId": "com.kiro.paint-color-assistant",
    "productName": "塗装色混合アシスタント",
    "directories": {
      "output": "dist-electron"
    },
    "files": [
      "frontend/dist/**/*",
      "electron-example/**/*"
    ]
  }
}
```